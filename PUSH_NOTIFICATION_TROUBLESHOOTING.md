# Push Notification Fix & Troubleshooting Guide\n\n## Diagnosis\n\n1. **Visit the diagnostic page**: `/push-diagnostic.html`\n2. **Check all system status indicators**\n3. **Run each test in order**\n4. **Download logs for analysis**\n\n## Common Issues & Fixes\n\n### Issue 1: OneSignal SDK Not Loaded\n**Symptoms**: OneSignal SDK shows as \"Not loaded\"\n**Cause**: SDK script not loaded in index.html or network issue\n**Fix**:\n- Verify `/public/index.html` includes OneSignal script tag\n- Check browser console for any 404 errors\n- Clear browser cache and reload\n- Check if OneSignalSDKWorker.js exists in public folder\n\n### Issue 2: Permission Denied\n**Symptoms**: Permission shows as \"denied\"\n**Cause**: User has blocked push notifications\n**Fix**:\n- In Chrome: Settings → Privacy & Security → Site Settings → Notifications\n- In Firefox: Preferences → Privacy & Security → Permissions → Notifications\n- Click the X next to xeevia.com and reload\n\n### Issue 3: No Player ID Generated\n**Symptoms**: OneSignal shows no player ID\n**Cause**: SDK not initialized or permission timing issue\n**Fix**:\n- Request permission first\n- Wait 2-3 seconds before checking player ID\n- Check browser console for [OneSignal] errors\n- Verify REACT_APP_ONESIGNAL_APP_ID is set\n\n### Issue 4: Service Worker Not Found\n**Symptoms**: Service Worker Registration fails\n**Cause**: SW script corrupted or not deployed\n**Fix**:\n- Verify `/service-worker.js` exists and is valid JS\n- Check browser DevTools → Application → Service Workers\n- Try hard refresh (Ctrl+Shift+R or Cmd+Shift+R)\n- Check for SW errors in console\n\n### Issue 5: Push Not Delivered\n**Symptoms**: No notifications received\n**Cause**: Could be multiple issues\n**Debug Steps**:\n```javascript\n// In browser console:\n\n// 1. Check OneSignal state\nawait window.OneSignal?.getDeviceState?.();\n\n// 2. Get player ID\nawait window.OneSignal?.User?.PushSubscription?.id?.();\n\n// 3. Check subscription\nconst reg = await navigator.serviceWorker.ready;\nconst sub = await reg.pushManager.getSubscription();\nconsole.log('Subscription:', sub?.endpoint?.substring?.(0, 60) + '...');\n\n// 4. Run full diagnostic\nawait pushService.diagnose();\n```\n\n## Configuration\n\n### Required Environment Variables\n\n```bash\n# .env.local or deployed secrets\nREACT_APP_ONESIGNAL_APP_ID=<your-app-id>\nREACT_APP_ONESIGNAL_SAFARI_WEB_ID=<your-safari-id>\n\n# Legacy (only used if fallback is needed)\nREACT_APP_VAPID_PUBLIC_KEY=<your-vapid-key>\n```\n\n### Supabase Database\n\nEnsure push_subscriptions table exists:\n```sql\nCREATE TABLE push_subscriptions (\n  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),\n  user_id UUID NOT NULL REFERENCES auth.users,\n  endpoint TEXT NOT NULL,\n  p256dh TEXT,\n  auth TEXT,\n  user_agent TEXT,\n  is_active BOOLEAN DEFAULT true,\n  updated_at TIMESTAMP DEFAULT now(),\n  created_at TIMESTAMP DEFAULT now(),\n  UNIQUE(user_id, endpoint)\n);\n```\n\n## Testing\n\n### Manual Test Flow\n\n1. **Clear all data**:\n   ```javascript\n   localStorage.clear();\n   sessionStorage.clear();\n   ```\n\n2. **Request permission**:\n   ```javascript\n   const perm = await Notification.requestPermission();\n   console.log('Permission:', perm);\n   ```\n\n3. **Check OneSignal**:\n   ```javascript\n   const playerId = await window.OneSignal?.User?.PushSubscription?.id?.();\n   console.log('Player ID:', playerId);\n   ```\n\n4. **Send test push**:\n   - Use OneSignal dashboard\n   - Or API call (see curl example below)\n\n### curl Test\n\n```bash\ncurl -X POST https://onesignal.com/api/v1/notifications \\\n  -H \"Content-Type: application/json; charset=utf-8\" \\\n  -H \"Authorization: Basic YOUR_REST_API_KEY\" \\\n  -d '{\n    \"app_id\": \"YOUR_APP_ID\",\n    \"include_player_ids\": [\"PLAYER_ID_FROM_USER\"],\n    \"headings\": {\"en\": \"Test Push\"},\n    \"contents\": {\"en\": \"This is a test notification\"},\n    \"priority\": 10\n  }'\n```\n\n## Advanced\n\n### Monitor Service Worker Messages\n\n```javascript\n// Listen for all SW messages\nif (navigator.serviceWorker?.controller) {\n  navigator.serviceWorker.onmessage = (event) => {\n    console.log('[SW Message]', event.data);\n  };\n}\n```\n\n### Check Browser Push API Status\n\n```javascript\nconst diagnostics = {\n  notificationAPI: 'Notification' in window,\n  serviceWorker: 'serviceWorker' in navigator,\n  pushManager: 'PushManager' in window,\n  permission: Notification.permission,\n  online: navigator.onLine,\n};\nconsole.table(diagnostics);\n```\n\n### Export Debug Logs\n\n```javascript\n// In browser console:\nimport { pushDebugLogger } from '/src/services/notifications/pushDebugLogger.js';\nconst logsJson = pushDebugLogger.exportLogs();\ncopy(logsJson); // Copy to clipboard\n```\n\n## Monitoring Checklist\n\n- [ ] OneSignal dashboard shows devices\n- [ ] push_subscriptions table has user records\n- [ ] Browser console has no errors\n- [ ] Service worker status is \"activated\"\n- [ ] Notification permission is \"granted\"\n- [ ] Device state shows push_subscription active\n- [ ] Network tab shows successful POST to OneSignal API\n- [ ] Local test notification works\n\n## Support\n\nIf push notifications still don't work:\n1. Visit `/push-diagnostic.html`\n2. Run all tests and download logs\n3. Check OneSignal dashboard for delivery status\n4. Verify user ID matches between app and subscription table\n5. Check Supabase logs for send-push edge function errors\n"}}]