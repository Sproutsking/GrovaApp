# Local React environment variables for development.
# Copy to `.env.local` and fill values you need for testing.

REACT_APP_DEV_AUTH_BYPASS_SECRET=XEEVIA_ACCESS_CODE_2026
# Optional: set a real user id in your dev/staging DB to map the bypass
# REACT_APP_DEV_AUTH_BYPASS_USER_ID=<uuid-of-dev-account>
