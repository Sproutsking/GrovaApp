import React, { useEffect, useState } from "react";
import { Check, Link2, RefreshCw, Radio } from "lucide-react";
import socialUpdatesService, { SOCIAL_PROVIDERS } from "../../../services/community/socialUpdatesService";

export default function UpdatesChannelPanel({ communityId, channelId, userId, isOwner }) {
  const [connections, setConnections] = useState([]);
  const [posts, setPosts] = useState([]);
  const [working, setWorking] = useState(false);
  const [error, setError] = useState("");
  const load = async () => { try { const [nextConnections, nextPosts] = await Promise.all([socialUpdatesService.listConnections(communityId), channelId ? socialUpdatesService.listPosts(channelId) : Promise.resolve([])]); setConnections(nextConnections); setPosts(nextPosts); } catch (e) { setError(e.message || "Could not load updates"); } };
  useEffect(() => { load(); }, [communityId]);
  const connectXeevia = async () => { setWorking(true); setError(""); try { await socialUpdatesService.connectXeevia(communityId, userId); await load(); } catch (e) { setError(e.message || "Could not connect Xeevia"); } finally { setWorking(false); } };
  return <section className="updates-panel"><div className="updates-head"><div className="updates-icon"><Radio size={21} /></div><div><span>Updates channel</span><h1>One stream for your community</h1><p>Xeevia posts are ready to flow here. Connect additional providers when their content permissions are configured.</p></div><button className="updates-refresh" onClick={load} title="Refresh"><RefreshCw size={15} /></button></div><div className="updates-providers">{SOCIAL_PROVIDERS.map((provider) => { const active = connections.some((item) => item.provider === provider.id && item.status === "active"); return <div className={`updates-provider${active ? " active" : ""}`} key={provider.id}><div className="provider-mark">{active ? <Check size={15} /> : <Link2 size={15} />}</div><div><strong>{provider.label}</strong><small>{active ? "Connected" : provider.ready ? "Available" : "Requires provider connection"}</small></div>{provider.id === "xeevia" && isOwner && !active && <button onClick={connectXeevia} disabled={working}>{working ? "..." : "Connect"}</button>}</div>; })}</div><div className="updates-feed"><div className="updates-feed-title">Latest updates <span>{posts.length}</span></div>{posts.length === 0 ? <div className="updates-empty">Connected sources will appear here when they publish.</div> : posts.map((post) => <article className="updates-post" key={post.id}><div className="updates-post-source">{post.provider}</div><h2>{post.content || "New update"}</h2><div className="updates-post-meta">{post.author_name || "Community source"}{post.published_at ? ` · ${new Date(post.published_at).toLocaleString()}` : ""}{post.permalink && <a href={post.permalink} target="_blank" rel="noreferrer">Open source</a>}</div></article>)}</div>{error && <div className="updates-error">{error}</div>}<style>{`.updates-panel{max-width:760px;margin:26px auto;padding:24px;border:1px solid rgba(96,165,250,.2);border-radius:20px;background:linear-gradient(145deg,rgba(17,28,40,.97),rgba(8,13,18,.99));box-shadow:0 20px 60px rgba(0,0,0,.3)}.updates-head{display:flex;align-items:flex-start;gap:14px}.updates-icon{width:46px;height:46px;border-radius:14px;display:flex;align-items:center;justify-content:center;color:#67e8f9;background:rgba(34,211,238,.1);border:1px solid rgba(34,211,238,.3);flex-shrink:0}.updates-head span{font-size:10px;color:#67e8f9;text-transform:uppercase;letter-spacing:.12em;font-weight:800}.updates-panel h1{margin:4px 0;color:#f1f8ff;font-size:22px}.updates-panel p{margin:0;color:#8fa3b5;font-size:12px;line-height:1.5}.updates-refresh{margin-left:auto;border:0;background:transparent;color:#7890a4;cursor:pointer}.updates-providers{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:22px}.updates-provider{display:flex;align-items:center;gap:9px;padding:12px;border-radius:11px;border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.03)}.updates-provider.active{border-color:rgba(34,211,238,.4);background:rgba(34,211,238,.08)}.provider-mark{width:28px;height:28px;border-radius:9px;display:flex;align-items:center;justify-content:center;color:#67e8f9;background:rgba(34,211,238,.1)}.updates-provider div:nth-child(2){display:flex;flex-direction:column;gap:3px;flex:1}.updates-provider strong{color:#edf7ff;font-size:12px}.updates-provider small{color:#7990a1;font-size:10px}.updates-provider button{border:1px solid rgba(103,232,249,.4);background:rgba(34,211,238,.12);color:#a5f3fc;border-radius:7px;padding:6px 8px;font-size:10px;font-weight:800;cursor:pointer}.updates-feed{margin-top:20px;border-top:1px solid rgba(255,255,255,.08);padding-top:14px}.updates-feed-title{display:flex;justify-content:space-between;color:#c8e8f2;font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.08em}.updates-feed-title span{color:#67e8f9}.updates-empty{padding:20px 0;color:#718493;font-size:12px}.updates-post{margin-top:8px;padding:13px;border-radius:11px;background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.08)}.updates-post-source{color:#67e8f9;font-size:9px;text-transform:uppercase;font-weight:800}.updates-post h2{margin:5px 0;color:#eef8ff;font-size:14px;font-weight:700}.updates-post-meta{display:flex;gap:8px;color:#7990a1;font-size:10px}.updates-post-meta a{color:#a5f3fc;margin-left:auto}@media(max-width:600px){.updates-panel{margin:14px;padding:16px}.updates-providers{grid-template-columns:1fr}.updates-post-meta{flex-wrap:wrap}}`}</style></section>;
}
