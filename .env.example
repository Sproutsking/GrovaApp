# Copy this file to .env and fill in your Supabase production credentials.
# This file is ignored by git, so it is safe to store local secret values here.

# Identity project
IDENTITY_SUPABASE_URL="https://pevhyriszemvnrwvfshm.supabase.co"
IDENTITY_SUPABASE_SERVICE_ROLE_KEY="<COPY FROM SUPABASE DASHBOARD>"
IDENTITY_DB_PASSWORD="<IDENTITY_DB_PASSWORD>"

# Core project
CORE_SUPABASE_URL="https://hhqohlzzpzgkfdeanudw.supabase.co"
CORE_SUPABASE_SERVICE_ROLE_KEY="<COPY FROM SUPABASE DASHBOARD>"
CORE_DB_PASSWORD="<CORE_DB_PASSWORD>"

# Wallet project
WALLET_SUPABASE_URL="https://wyqtcjqbdniwebvrwdnk.supabase.co"
WALLET_SUPABASE_SERVICE_ROLE_KEY="<COPY FROM SUPABASE DASHBOARD>"
WALLET_DB_PASSWORD="<WALLET_DB_PASSWORD>"

# Optional: use the production schema export from exports/old_project.
# SCHEMA_VERSION=production
# EXPORT_DIR=exports/old_project
# BOUNDARY_MAP_FILE=boundary_map.json
# Set SKIP_SCHEMA_APPLY=1 if you cannot reach Supabase Postgres directly and have already applied schema manually.
# SKIP_SCHEMA_APPLY=1
# To bypass schema apply entirely and import only, run:
#   bash scripts/import_only.sh
